{% comment %}theme-check-disable ImgLazyLoading{% endcomment %}
{% liquid
  if collection.handle == 'all'
    assign title = 'ALL' | escape
  else
    assign title = collection.title | escape
  endif
  assign shop_title = title | prepend: 'Shop&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;'
  assign filters1 = section.settings.filters
  assign filters = section.settings.product-colletion-filter
  
%}
{% comment %} test {% endcomment %}

<div
  class="collection-page__header-container">
  {% render 'heading' 
   with title: shop_title,
   title_tag: 'h1',
   class: 'collection-page',
   animations_reveal_on_scroll: section.settings.animations_reveal_on_scroll,
   enable_filter: section.settings.enable_filter,
   filters: filters %}
</div>


{% schema %}
  {
    "name": "t:sections.main-collection-banner.name",
    "class": "section",
    "settings": [
      {
        "type": "paragraph",
        "content": "t:sections.main-collection-banner.settings.paragraph.content"
      },
      {
        "type": "header",
        "content": "Filter"
      },
      {
        "type": "collection_list",
        "id": "filters",
        "label": "Collection Filters"
      },
      {
        "type": "link_list",
        "id": "product-colletion-filter",
        "label": "Filter"
      }, {
        "type": "checkbox",
        "id": "enable_filter",
        "label": "Enable Filter"
      }, {
        "type": "color_scheme",
        "id": "color_scheme",
        "label": "t:sections.all.colors.label",
        "default": "background-1"
      }
    ]
  }
{% endschema %}